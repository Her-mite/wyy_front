const routes = [
    {
        path: '/',
        exact: true,
       
    },
    {
        path: '/pages',
        exact: true,

        
    },
]

routes.map((value, item)=>{
    console.log(value, item
        );
})